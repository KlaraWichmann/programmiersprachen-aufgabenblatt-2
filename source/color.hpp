#ifndef COLOR_HPP
#define COLOR_HPP

//struct defining a rgb color, default is set to 0.5, values between 0.0 and 1.0

struct Color {
    float r = 0.5;
    float g = 0.5;
    float b = 0.5;
};


#endif // COLOR_HPP
